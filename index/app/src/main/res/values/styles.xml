<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textSize">25dp</item>
        <item name="android:typeface">monospace</item>
        <item name="android:itemBackground">@color/action_background</item>
    </style>

    <!-- NavigationView menu的title-->
    <style name="TextAppearance44">
        <item name="android:textColor">#d4d4d4</item>
        <item name="android:textSize">25sp</item>
    </style>

    <!-- NavigationView menu-->
    <style name="NavigationDrawerStyle">
        <item name="android:textSize">25sp</item>
        <item name="android:typeface">monospace</item>
        <item name="android:listPreferredItemHeightSmall">60dp</item>
    </style>

    <style name="ArTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="popupMenuStyle">@style/PopupMenu</item>
        <item name="android:typeface">monospace</item>
        <item name="android:textSize">22sp</item>
    </style>

    <style name="PopupMenu" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@drawable/mback1</item>
        <item name="android:typeface">monospace</item>
        <item name="android:dropDownVerticalOffset">1dp</item>
        <item name="android:dropDownHorizontalOffset">6dp</item>
    </style>

</resources>
